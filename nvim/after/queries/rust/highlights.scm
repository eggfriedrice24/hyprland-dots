;; extends

; Struct field declarations and literal keys as @property (rose) to
; match the theme's declarations-rose / access-blue pattern from TS.
(field_declaration
  name: (field_identifier) @property)

(field_initializer
  field: (field_identifier) @property)
