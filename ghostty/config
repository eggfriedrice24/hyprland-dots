# Modular config - ghostty loads included files after this one, in order,
# so later files win conflicts. Keep this file includes-only.
config-file = font
config-file = appearance
config-file = behavior
config-file = keybinds

# per-machine overrides, untracked (e.g. laptop font size)
config-file = ?local
